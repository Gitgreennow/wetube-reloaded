extends base.pug 



block content 
    h4 Change Title of video
    form(method="POST")
        input(name="title",placeholder="Video Titile", value=video.title,required)
        input(value="Save",type="submit")